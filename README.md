# Peterson Service website

Static website for Peterson Service, published on GitHub Pages at `peterson-services.org`.

## Sitemap maintenance

Run `node scripts/generate-sitemap.mjs` after adding or removing gallery media. The script keeps `sitemap.xml` aligned with the canonical pages and JavaScript-powered image gallery.
